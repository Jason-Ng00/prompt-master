# Dev log - 2026-04-04

## Work Done
1. **Fix: User Message Visibility in Chat**:
   - Discovered an asynchronous `setState` overwrite issue in React where consecutive `addChatMessage` calls in `handleSendPrompt` resulted in the assistant message overriding the user message before the state officially committed.
   - Fixed by grouping all new chat messages (`user` and `assistant`) into an array and doing a single atomic functional `setUserData(prev => ...)` update.

2. **Feature: Light / Dark Mode Toggle**:
   - Extracted hard-coded colors into CSS variables for light vs dark configurations via `[data-theme='dark']` dataset property.
   - Lightened up the grey text shades (`--text-muted`) in dark mode config to improve readability across the application.
   - Added a slick "Toggle Theme" button in the global navbar, persisting user preference to `localStorage`.

3. **Feature: Delete Project**:
   - Added a `handleDeleteProject` method gracefully filtering out the deleted project.
   - Integrated a small delete icon (`✕` button) on project items upon hover in the sidebar layout.

4. **UX Enhancements**:
   - Implemented an animated LLM loading state (`setIsLoading`) with smooth typing dots when the assistant processes. 
   - Added forced smooth automatic scrolling (`chatEndRef`) so the chat reliably snaps to the new messages instantly.
   - Deployed a custom Prompt Engineering Template Dropdown directly on the Final Prompt Panel (Standard, Chain of Thought, Expert Persona, Tutorial) that wraps the `latestFinalPrompt` before UI presentation and clipboard access.

5. **LLM API Wiring & UI Polish**:
   - Successfully wired up the actual **Gemini 2.5 Flash API** using the key configured as `VITE_GOOGLE_GEMINI_API_KEY` in the `.env` file. The backend logic now sends robust context history along with custom `systemInstructions` to instruct the LLM to output `FINAL_PROMPT: ` logic.
   - Redesigned the project navigation sidebar text CSS variables to provide explicit high-contrast coloring (`--sidebar-name-color` and `--sidebar-desc-color`), perfectly resolving visibility issues in dark mode.
   - Reduced bulk in the chat conversation footer by restructuring the Send/Clear interfaces into minimal inline circular icon buttons right next to the new pill-shaped input field.

